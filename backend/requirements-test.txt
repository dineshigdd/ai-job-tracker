-r requirements.txt

# Testing
pytest==8.0.0
pytest-asyncio==0.23.5  # without it, every @pytest.mark.asyncio test is silently skipped
pytest-cov==4.1.0

# HTTP client for testing
httpx==0.27.0

# PDF handling (also in requirements.txt)
pypdf==6.15.0
python-multipart==0.0.9